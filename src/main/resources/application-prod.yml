server:
  port: 8091
spring:
  application:
    name: seal-login-service
  cloud:
    consul:
      discovery:
        health-check-path: /seal/health #检测实例健康
        health-check- interval: 10s
        #每隔1es检查
        hostname: localhost
        #配置实例地址
        register: true # 自动注册
        service-name: ${spring.application.name} #实例名称
      host: localhost
      port: 8500
  datasource:
    username: root
    password: gzou
    url: jdbc:mysql://localhost:3306/carsale?useUnicode=true&characterEncoding=utf-8
    #driver-class-name: com.mysql.jdbc.Driver 这个驱动已经被弃用
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis:
  mapper-locations: classpath:mappers/*.xml
  type-aliases-package: com.gxyan.pojo
